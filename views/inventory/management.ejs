<!-- Content Heading -->
<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<!-- Message Block -->
<%- messages() %>

<% if (errors) { %>
    <ul class="notice">
    <% errors.array().forEach(error => { %>
        <li><%= error.msg %></li>
    <%  }) %>
    </ul>
<% } %>

<div id="new-features-links">
    <a href="/inv/add-classification" title="Click to add classification">Add New Classification</a><br>
    <a href="/inv/add-inventory" title="Click to add new vehicle">Add New Vehicle</a>
</div>

  <h2>Manage Inventory</h2>
  <p>Select a classification from the list to see items belonging to it.</p>

    <table id="inventoryDisplay"></table>
    <noscript>JavaScript must be enabled to use this page.</noscript>


<script src="../../js/inventory.js"></script>